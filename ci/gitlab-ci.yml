stages:
  - review

code_review:
  stage: review
  image: python:3.11
  script:
    - pip install -r requirements.txt
    - python scripts/review_agent.py --mr "$CI_MERGE_REQUEST_IID"
    - python scripts/post_comments.py --mr "$CI_MERGE_REQUEST_IID"
